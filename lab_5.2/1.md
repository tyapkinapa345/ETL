Ошибка `NotJSONError` означает, что файл `ml (1).ipynb` не является корректным Jupyter Notebook — внутри него, скорее всего, обычный Python-код, а не JSON-структура с ячейками. Вы, вероятно, скопировали код из моего предыдущего сообщения в текстовый файл и переименовали его в `.ipynb`. Jupyter не может его прочитать.

## 🔧 Исправление: создайте новый ноутбук правильно

### Способ 1 (рекомендованный): создать новый ноутбук через интерфейс Jupyter

1. Откройте Jupyter: http://localhost:8888
2. Нажмите **New** → **Python 3 (ipykernel)** — создастся новый пустой ноутбук.
3. Назовите его `ml.ipynb` (переименуйте).
4. Скопируйте код из **моего предыдущего ответа** (тот большой JSON с ячейками) **НЕ НУЖНО**. Вместо этого, пошагово создайте ячейки вручную:

**Ячейка 1 (Markdown):**
```markdown
# 🚀 ML-классификация ракет (CLIP Model)
## Вариант 16: Rocket Launch Analytics
```

**Ячейка 2 (Code):**
```python
import os
import pandas as pd
import torch
from PIL import Image
from transformers import CLIPProcessor, CLIPModel
from datetime import datetime
import warnings
warnings.filterwarnings('ignore')
print("✅ Библиотеки импортированы")
```

**Ячейка 3 (Code):**
```python
# Определяем пути
if os.path.exists('/opt/airflow'):
    BASE_DIR = '/opt/airflow'
else:
    BASE_DIR = os.getcwd()

IMAGES_DIR = os.path.join(BASE_DIR, 'data', 'images')
OUTPUT_FILE = os.path.join(BASE_DIR, 'data', 'ml_predictions.csv')

print(f"🖼️ Images dir: {IMAGES_DIR}")
print(f"📄 Output: {OUTPUT_FILE}")
os.makedirs(IMAGES_DIR, exist_ok=True)
```

**Ячейка 4 (Code):**
```python
image_files = [os.path.join(IMAGES_DIR, f) for f in os.listdir(IMAGES_DIR) if f.lower().endswith(('.jpg','.jpeg','.png'))]
print(f"Найдено изображений: {len(image_files)}")
if len(image_files)==0:
    raise Exception("Нет изображений! Запустите DAG в Airflow.")
```

**Ячейка 5 (Code):**
```python
MODEL_NAME = "openai/clip-vit-base-patch32"
ROCKET_LABELS = ["Falcon 9 rocket","Falcon Heavy rocket","Soyuz rocket","Ariane 5 rocket","Electron rocket","Long March rocket","Atlas V rocket","Delta IV rocket","Antares rocket","Starship rocket","Space Shuttle","launch vehicle"]

print("Загрузка модели CLIP...")
model = CLIPModel.from_pretrained(MODEL_NAME)
processor = CLIPProcessor.from_pretrained(MODEL_NAME)
device = "cuda" if torch.cuda.is_available() else "cpu"
model = model.to(device)
print(f"Модель на {device}")
```

**Ячейка 6 (Code):**
```python
def classify_image(image_path):
    try:
        image = Image.open(image_path).convert("RGB")
        inputs = processor(text=ROCKET_LABELS, images=image, return_tensors="pt", padding=True).to(device)
        with torch.no_grad():
            probs = model(**inputs).logits_per_image.softmax(dim=1)[0]
        top_prob, top_idx = probs.max(dim=0)
        return {"image_name": os.path.basename(image_path), "predicted_rocket": ROCKET_LABELS[top_idx.item()], "confidence": round(top_prob.item()*100,2), "error": None}
    except Exception as e:
        return {"image_name": os.path.basename(image_path), "predicted_rocket": None, "confidence": 0, "error": str(e)}
```

**Ячейка 7 (Code):**
```python
results = []
for i, img in enumerate(image_files,1):
    print(f"[{i}/{len(image_files)}] {os.path.basename(img)}...", end=" ")
    res = classify_image(img)
    results.append(res)
    if res["error"]: print(f"❌ {res['error']}")
    else: print(f"✅ {res['predicted_rocket']} ({res['confidence']}%)")
df = pd.DataFrame(results)
df.to_csv(OUTPUT_FILE, index=False)
print(f"\n✅ Сохранено в {OUTPUT_FILE}")
```

**Ячейка 8 (Code):**
```python
print(df[['image_name','predicted_rocket','confidence']].head())
```

5. Нажмите **Kernel → Restart & Run All**.

### Способ 2 (быстрый): загрузить правильный JSON-файл

Я подготовил для вас готовый корректный `ml.ipynb` в виде JSON. Скачайте его по ссылке (если не можете — создайте вручную по инструкции выше). В Jupyter нажмите **Upload** и выберите скачанный файл.

**Ссылка на готовый ноутбук:** (я не могу прикрепить файл, но могу дать команду для создания через терминал)

Выполните в терминале на ВМ (в папке проекта):

```bash
cat > ml_correct.ipynb << 'EOF'
{
 "cells": [
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import os, pandas as pd, torch\n",
    "from PIL import Image\n",
    "from transformers import CLIPProcessor, CLIPModel\n",
    "from datetime import datetime\n",
    "warnings.filterwarnings('ignore')\n",
    "print('OK')"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 4
}
EOF
```

Это минимальный ноутбук, затем вы можете добавлять ячейки через интерфейс.

## 🚀 После исправления

- Запустите все ячейки.
- Убедитесь, что в `data/images/` есть файлы (если нет — запустите DAG в Airflow).
- После успешного выполнения откройте Streamlit: http://localhost:8501

Если останутся ошибки — покажите скриншот или текст новой ошибки.
